<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thesis.android_challenge_w3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidChallengeW3">
        <activity android:name=".activity.splash.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.boarding.BoardingOneActivity"/>
        <activity android:name=".activity.boarding.BoardingTwoActivity"/>
        <activity android:name=".activity.boarding.BoardingThreeActivity"/>
        <activity android:name=".activity.welcome.WelcomeActivity"/>

        <activity android:name=".activity.signup.SignUpActivity"/>
        <activity android:name=".activity.signin.SignInActivity"/>
        <activity android:name=".activity.profile.ProfileActivity"/>
    </application>
</manifest>